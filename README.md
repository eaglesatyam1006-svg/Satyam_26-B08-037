Day 1 (Aug 29): Set up GitHub repo, practiced git add/commit/push/pull/clone, 
created/renamed/deleted files and folders via CLI, recovered a deleted file 
from a previous commit. Faced a PowerShell error with git checkout syntax 
(angle brackets in placeholders) and an "invalid reference" error from a bad 
commit hash — fixed by copying exact hashes from git log --oneline.